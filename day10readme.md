How it Works:
Keep track of:
current_sum → current running sum
max_sum → maximum sum found so far
At every step:
Either start a new subarray
Or continue the existing one

The algorithm efficiently solves the problem in:

Time Complexity: O(n)
Space Complexity: O(1)